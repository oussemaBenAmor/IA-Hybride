# ===================================================================
# Dépendances du projet — Conseiller Bancaire Hybride (LLM + BRMS/ODM)
# Versions figées depuis l'environnement de développement.
# Installation :  pip install -r requirements.txt
# ===================================================================

# ── API / Orchestration (backend FastAPI) ──────────────────────────
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-multipart==0.0.29
httpx==0.27.2
tenacity==8.5.0
python-dotenv==1.2.2

# ── Pipeline LLM / LangGraph ───────────────────────────────────────
langgraph==1.2.0
langgraph-checkpoint==4.1.0
langgraph-checkpoint-postgres==3.1.0
langchain-core==1.4.0
langchain-ollama==1.1.0
ollama==0.6.2

# ── Embeddings / NLP ───────────────────────────────────────────────
sentence-transformers==5.5.1
transformers==5.9.0
torch==2.12.0
scikit-learn==1.8.0
numpy==2.4.6

# ── Correction orthographique ──────────────────────────────────────
pyspellchecker==0.9.0
symspellpy==6.9.0
wordfreq==3.1.1
RapidFuzz==3.14.5

# ── Base de données PostgreSQL + pgvector ──────────────────────────
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1
psycopg2-binary==2.9.12
pgvector==0.4.2

# ── Authentification (JWT + hachage des mots de passe) ─────────────
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# ── Frontend (Streamlit + visualisations) ──────────────────────────
streamlit==1.57.0
plotly==6.9.0
pandas==3.0.3

# ── Fichiers Excel (scénarios de test / rapports) ──────────────────
openpyxl==3.1.5

# ── Tests & couverture ─────────────────────────────────────────────
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1